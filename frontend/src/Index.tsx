import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

/**
 * Bind the App component to the browser's DOM
 */
ReactDOM.render(
    <App />,
    document.getElementById('root'),
);